---
title: "useEnhancedToast"
category: "hook"
summary: '"react-toastify" toast method, powered by i18n'
---

### Source

```javascript
import { ReactText } from "react";
import { useTranslation } from "react-i18next";
import { toast, ToastContent, ToastOptions } from "react-toastify";

export default useEnhancedToast;

const useEnhancedToast = () => {
  const { t } = useTranslation();

  /**
   * @param {ToastContent} content
   * @param {ToastOptions<{}>} options
   * @returns {ReactText}
   */
  return function (content, options) {
    const toastContent = typeof content === "string" ? t(content) : content;
    return toast(toastContent, options);
  };
};
```

### Example

```javascript
const toast = useEnhancedToast();
const toastId = toast("a.b", { type: "info" });
```
